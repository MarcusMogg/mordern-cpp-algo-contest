#pragma once

#include <vector>

namespace leetcode::twosum {
std::vector<int> Solve(const std::vector<int>& nums, const int& target);

namespace standard {
std::vector<int> Solve(const std::vector<int>& nums, const int& target);
}  // namespace standard

}  // namespace leetcode::twosum